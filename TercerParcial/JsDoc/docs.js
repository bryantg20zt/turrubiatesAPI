/**
 * Represents a book.
 * @constructor
 * @param {string} name - The title of the driver.
 * @param {string} lastName - The author of the driver.
 */
export function Driver(name, lastName) {
    console.log(`Driver ${name} ${lastName} created`);
}